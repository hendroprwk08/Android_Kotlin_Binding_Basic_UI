<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidKotlinBindingBasicUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryShade</item>
        <item name="colorAccent">@color/accent</item>

        <!-- menu style -->
        <item name="android:textColor">@color/black</item>
        <item name="android:panelFullBackground">@color/primaryTint</item>
    </style>

    <style name="Theme.AndroidKotlinBindingBasicUI" parent="Base.Theme.AndroidKotlinBindingBasicUI" />
</resources>