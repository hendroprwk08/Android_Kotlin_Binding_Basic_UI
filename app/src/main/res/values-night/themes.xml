<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidKotlinBindingBasicUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryShade</item>
        <item name="colorAccent">@color/accent</item>

        <!-- menu style -->
        <item name="android:textColor">@color/white</item>
        <item name="android:panelFullBackground">@color/cardview_dark_background</item>
    </style>
</resources>